import { Button } from '@mui/material';

export const CustomButton = () => {
  return (
    <Button
      className='!bg-[#1F5AF4] !py-3 !capitalize !rounded-none !shadow-none'
      variant='contained'
    >
      Login
    </Button>
  );
};
